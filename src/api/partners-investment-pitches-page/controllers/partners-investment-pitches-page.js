'use strict';

/**
 * partners-investment-pitches-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::partners-investment-pitches-page.partners-investment-pitches-page');
