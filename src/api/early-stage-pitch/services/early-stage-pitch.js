'use strict';

/**
 * early-stage-pitch service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::early-stage-pitch.early-stage-pitch');
