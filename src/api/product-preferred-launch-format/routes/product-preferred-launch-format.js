'use strict';

/**
 * product-preferred-launch-format router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-preferred-launch-format.product-preferred-launch-format');
