'use strict';

/**
 * product-preferred-launch-format controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::product-preferred-launch-format.product-preferred-launch-format');
