'use strict';

/**
 * product-preferred-launch-format service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::product-preferred-launch-format.product-preferred-launch-format');
